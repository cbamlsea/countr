# countr

Learning Rust by rewriting coreutils, one tool at a time — just a thing i made

## install

```bash
cargo build --release
```

## usage

```bash
./target/release/countr src/*.rs
cat README.md | ./target/release/countr
```
